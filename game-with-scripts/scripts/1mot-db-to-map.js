// import objectToMapSource from './object-to-map-source.js';
// import fs from "fs";
// import { join, dirname } from 'path';
// import { fileURLToPath } from 'url';
// import db from '../../packages/game-core/data/1mot.json' with { type: "json" };

// const __dirname = dirname(fileURLToPath(import.meta.url));

// const DIR = join(__dirname, '..', '..', 'packages', 'game-core', 'data');

// fs.writeFileSync(join(DIR, '1mot-map.js'), objectToMapSource(db));

// SHOULD NOT BE NECESSARY. NO REAL PERF GAIN